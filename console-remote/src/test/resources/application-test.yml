debug: true

env:
  otlp:
    enabled: true # テストはOTELは無効なのを前提にしているためfalse固定すること

spring:
  main:
    banner-mode: off

rms:
  debug:
    configdump:
      enable: false
      #system-properties: on
      #system-environment: on
